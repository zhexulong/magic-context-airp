{
  "name": "@cortexkit/opencode-magic-context",
  "version": "0.42.3",
  "type": "module",
  "description": "OpenCode plugin for Magic Context — cross-session memory and context management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "ualtinok",
  "license": "MIT",
  "keywords": [
    "opencode",
    "plugin",
    "context",
    "memory",
    "ai",
    "llm",
    "prompt-caching",
    "session-history"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cortexkit/magic-context",
    "directory": "packages/plugin"
  },
  "files": [
    "dist",
    "index.js",
    "src/tui",
    "src/tui-compiled",
    "src/shared",
    "src/config",
    "src/agents",
    "src/features/magic-context/dreamer/cron.ts",
    "src/features/magic-context/dreamer/task-registry.ts",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p ../retina-local-fs/tsconfig.build.json && bun run build:tui && rm -f dist/index.js dist/transformers-web.js dist/transformers-node-wasm.js dist/*-*.js && bun build src/features/magic-context/memory/transformers-web-entry.ts --outfile dist/transformers-web.js --target browser --format esm --external onnxruntime-web && bun scripts/build-transformers-node-wasm.ts dist && bun build src/index.ts --outdir dist --target node --format esm --splitting --external @opencode-ai/plugin --external onnxruntime-node --external onnxruntime-web --external sharp --external bun:sqlite --external node:sqlite && bun run build:v2 && tsc --emitDeclarationOnly",
    "build:v2": "bun build src/v2/server.ts --outfile dist/v2/server.js --target node --format esm --external @opencode/plugin && bun test src/v2/server.test.ts",
    "build:tui": "bun scripts/build-tui.ts",
    "check:tui-compiled": "bun run build:tui && git diff --exit-code -- src/tui-compiled && test -z \"$(git status --porcelain -- src/tui-compiled)\"",
    "check:transformers-node-wasm": "bun scripts/verify-transformers-node-wasm.ts",
    "typecheck": "tsc -p ../retina-local-fs/tsconfig.build.json && tsc --noEmit && tsc -p tsconfig.scripts.json",
    "test": "bun install --frozen-lockfile && bun test --parallel --timeout 30000",
    "test:serial": "bun install --frozen-lockfile && bun test --timeout 30000",
    "test:parallel:probe": "bun scripts/verify-parallel-db-isolation.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "clean": "rm -rf dist",
    "prepublishOnly": "bun run build && bun run build:tui"
  },
  "dependencies": {
    "@cortexkit/subc-client": "0.11.1",
    "@huggingface/transformers": "^4.2.0",
    "@jitl/quickjs-singlefile-cjs-release-asyncify": "0.32.0",
    "@opencode-ai/plugin": "^1.17.11",
    "@opencode-ai/sdk": "^1.17.11",
    "@opentui/core": "^0.4.2",
    "@opentui/solid": "^0.4.2",
    "ai-tokenizer": "^1.0.6",
    "comment-json": "^5.0.0",
    "jsonc-parser": "^3.3.1",
    "onnxruntime-web": "1.26.0-dev.20260416-b7804b056c",
    "quickjs-emscripten": "^0.32.0",
    "solid-js": "1.9.12",
    "zod": "^4.1.8"
  },
  "optionalDependencies": {
    "onnxruntime-node": "1.24.3",
    "sharp": "^0.35.0"
  },
  "devDependencies": {
    "@opencode/plugin": "2.0.3",
    "@opencode/cli": "2.0.3",
    "@opencode/client": "2.0.3",
    "effect": "4.0.0-rc.112",
    "@ai-sdk/anthropic": "3.0.82",
    "@huggingface/transformers": "^4.1.0",
    "@ai-sdk/openai-compatible": "2.0.41",
    "@biomejs/biome": "^2.5.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.10",
    "@types/node": "^22.20.0",
    "bun-types": "^1.3.10",
    "typescript": "^5.8.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./tui": {
      "types": "./src/tui/index.tsx",
      "import": "./src/tui/entry.mjs"
    }
  },
  "oc-plugin": [
    "server",
    "tui"
  ],
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.15.0"
  }
}
