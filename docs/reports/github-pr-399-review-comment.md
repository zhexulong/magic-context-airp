Requesting two focused changes before merge. The Pi diagnosis and implementation are strong; in particular, finding that the empty-assistant branch discarded `finalErrorMessage` is excellent, and I reproduced the exact OpenAI failure (`gpt-5.4`: empty content, `stopReason:error`, `Unsupported parameter: temperature`) followed by a successful `PONG` when temperature was absent.

1. Please pin the two root-cause removals themselves. I restored both `historian?.temperature ?? 0.1` in `index.ts` and `temperature = 0.1` in `pi-historian-runner.ts` under a negative-control marker, and all 93 PR-focused tests still passed. A resolver test should prove omitted → `undefined` and explicit `0.1` → `0.1`; a runner test should independently prove an omitted value stays omitted at the spawned attempt. Each test should fail if its corresponding default returns.
2. Please narrow the common docs/description to the Pi leg. This PR does not make temperature opt-in repository-wide: OpenCode still adds `temperature: 0.1` in `resolveHistorianAgentOverrides`, and Rust still unconditionally sends `generation.temperature=0.1` to Broca. The docs sentence “Reasoning models reject the parameter outright” is also too absolute; Anthropic thinking constrains it to `1`. “Some reasoning models reject or constrain temperature” is precise.

I do **not** recommend expanding this contributor PR into the other runtime legs. I will track a maintainer follow-up to (a) remove OpenCode's implicit default while preserving explicit flash calibration and (b) make Rust's Broca generation temperature optional, with absent/explicit request-shape tests across fallback attempts.

Other review points are green: explicit Pi `historian.temperature` still reaches every calibrated payload shape; max-output and temperature calibration are independent; legitimate empty `stop` completions remain `no_assistant`; fallback eligibility and the isolated-retry marker are unchanged; the focused suite is 93/93; typecheck and lint pass; the lockfile, manifests, and fences are untouched. The full Pi suite was 885/886 with only the unrelated 250k-tail p95 timing assertion over budget.
